/**
 * 占位常量与对话兜底文案
 * ===========================================================
 * ⚠️ 隐私说明
 * 设计稿的「我的」页里硬编码了真实姓名与手机号。这些**没有**复制到这里 ——
 * 本文件全部使用明确的中性占位值(示例昵称 / 138****0000 / example.com 邮箱)。
 * 原因:代码要推到 GitHub,个人信息一旦进仓库就会留在提交历史里,事后删除也清不掉。
 *
 * 这里曾经还存着首页、日记页、结果页的全部展示数据(TODAY / MEALS_TODAY /
 * DIARY_WARNING / ANALYSIS)。那些已经全部删掉了 —— 那些页面的数字现在由
 * src/store/derive.ts 从餐次记录现算。留着常量会让人分不清哪个才是真的数据源。
 *
 * 现在只剩两类东西:
 *   1. ACCOUNT —— 账号相关的占位展示值,演示版没有真实账号体系
 *   2. 对话相关 —— 无 Key 时的降级回复,接上真实 agent 后仅作为兜底
 */

export interface ChatMessage {
  id: string
  role: 'user' | 'assistant'
  content: string
}

/* ------------------------------------------------------------
   账户(全部为占位数据,勿替换为真实信息)
   ------------------------------------------------------------ */

export const ACCOUNT = {
  /** 占位昵称 —— 不要改成真实姓名 */
  nickname: '示例用户',
  /** 占位手机号 —— 已脱敏且为不可能存在的号段 */
  phone: '138****0000',
  /** 占位邮箱 —— example.com 为 RFC 保留域名,永不对应真实邮箱 */
  email: 'user@example.com',
  plan: '免费版',
} as const

/* ------------------------------------------------------------
   对话
   ------------------------------------------------------------ */

/**
 * 空会话的开场白。
 *
 * ⚠️ **2026-09-24 重写过一次。** 原来那句是「也可以直接问我**任何**饮食问题」——
 * 一个「任何」把边界抹掉了：用户于是拿闲聊、医疗、用药来问，而**膳享+ 那边
 * 当时没有「不归我管」这条出口**（现在补在 `dify/out-of-scope-prompt.txt` 里），
 * 它只能硬塞进一个吃的 mode，于是编出一道菜。开场白不该替它许下做不到的承诺。
 *
 * 现在两句各管一件事：第一句说**能做什么**（拍照算营养 / 打字问），第二句说
 * **照谁的档案答** —— 后半句是膳享+ 真正比别人多出来的那一层，原来那句里没有。
 *
 * 结尾那个「今天想从哪开始？」也去掉了：输入框上方那三个快捷提问
 * （`QUICK_QUESTIONS`）已经在做同一件事，两句问话叠在一起是重复。
 */
export const CHAT_GREETING: ChatMessage = {
  id: 'c0',
  role: 'assistant',
  content:
    '我是食衡。拍一张这一餐的照片，我帮你算营养和风险；也可以直接问我饮食上的事 —— 我照着你的健康档案答。',
}

/** 演示用的预置对话 —— 接入 Dify 后由真实 agent 回复替代 */
export const CHAT_DEMO: ChatMessage[] = [
  { id: 'c1', role: 'user', content: '午餐吃了红烧肉，有点咸，怎么补救？' },
  {
    id: 'c2',
    role: 'assistant',
    content:
      '红烧肉属于高钠高脂菜品，结合你的「控盐」目标，建议：\n· 晚餐以清蒸、白灼为主，不再加盐\n· 搭配一份绿叶菜（如油麦菜、菠菜）补钾，帮助钠排出\n· 全天饮水 1500ml 以上\n依据《中国居民膳食指南 2022》。',
  },
]

/** 快捷提问 —— 输入框上方的建议问题 */
export const QUICK_QUESTIONS = ['这餐热量高吗？', '晚餐吃什么合适？', '怎么补钾？']
