# ============================================
#  依赖
# ============================================
node_modules/
.pnp
.pnp.js

# ============================================
#  构建产物
# ============================================
dist/
dist-ssr/
build/
*.tsbuildinfo

# ============================================
#  日志
# ============================================
logs/
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# ============================================
#  编辑器 / 系统
# ============================================
.vscode/*
!.vscode/extensions.json
.idea/
.DS_Store
Thumbs.db
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# ============================================
#  ⚠️ 隐私数据 / 凭据 —— 严禁提交
# ============================================
# 本目录存放一切敏感信息:API Token、密钥、个人健康数据等
private/

# 环境变量(只提交 .env.example 模板)
.env
.env.*
!.env.example

# 其他可能的凭据文件
*.token
*.key
*credentials*
*secret*

# ============================================
#  Figma 设计导出
# ============================================
# design/tokens.json 只有颜色/字号/间距,不含文字,可以公开。
# 原始导出与结构摘要含设计稿的全部文字(含账号昵称),已改写到 private/design/。
# 下面两行是兜底:万一有人手动在 design/ 下重新生成了它们,也不会被提交。
design/figma-raw.json
design/figma-summary.txt

# ============================================
#  知识库原始素材 —— 不进仓库
# ============================================
# knowledge-base/ 是生成知识库文档用的**原始素材**:官方食养指南 PDF、
# 提取出来的 .txt、整理后的 .md,以及一本正式出版的图书的 PDF。
#
# 两条理由,任何一条单独成立就够:
#   1. 体积 —— 光 PDF 就 120MB 以上,推上去会让 clone 慢到没人愿意试;
#      GitHub 对单文件还有 100MB 的硬上限
#   2. 版权 —— 官方指南是可以引用的公开文件,但那本图书不是。
#      这个仓库是要给面试官点开看的,不该顺带分发别人的书
#
# 生成物 dify/knowledge/ 是**另外一回事**,它进仓库:那是喂给 Dify 的成品,
# 体积小,而且读的人需要它才能看懂「路径 B」是怎么落地的。
knowledge-base/
